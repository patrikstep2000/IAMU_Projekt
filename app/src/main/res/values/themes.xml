<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PetShop" parent="Theme.MaterialComponents.Light.NoActionBar.Bridge">
        <!-- Primary brand color. -->
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="Theme.PetShop.Main" parent="Theme.MaterialComponents.Light.DarkActionBar.Bridge">
        <!-- Primary brand color. -->
        <item name="colorOnPrimary">@color/white</item>
    </style>

    <style name="SplashScreenTheme" parent="Theme.PetShop">
        <item name="android:textSize">@dimen/splashTextSize</item>
        <item name="android:textStyle">bold</item>
        <item name="android:fontFamily">cursive</item>
        <item name="android:navigationBarColor">@color/white</item>
    </style>
</resources>